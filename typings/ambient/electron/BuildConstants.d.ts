interface BuildConstants {
  /**
   * The path to the HTML file which bootstraps the Electron renderer process.
   * Only available in the Electron main process!
   */
  ELECTRON_RENDERER_HTML_PATH: string;
}
