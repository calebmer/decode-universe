#!/bin/bash

$(npm bin)/concurrently \
  '$(npm bin)/webpack-dev-server --config ./webpack.config.renderer.js --port 1998' \
  '$(npm bin)/tsc --project ./src/main/tsconfig.json && $(npm bin)/electron .'
