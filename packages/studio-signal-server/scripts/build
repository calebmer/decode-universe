#!/bin/bash

rm -rf build

$(cd ../.. && npm bin)/tsc

# Cleanup the build directory. We import some types from another TypeScript
# package, so TypeScript ends up building that package too. We don’t want that
# and so need to remove it.
rm -rf build/studio-signal-client
mv build/studio-signal-server/src/* build
rm -rf build/studio-signal-server
