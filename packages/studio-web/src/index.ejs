<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Decode Studio</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <!-- TODO: Self host fonts to be available offline? -->
    <link href="https://fonts.googleapis.com/css?family=Montserrat:400,500,700" rel="stylesheet">
    <style>
/*
 * The default CSS used in the application. Most styles will be added by the
 * JavaScript code. We have no CSS reset and no global styles. This is so that
 * our components can be used in any location as they are forced to override the
 * global styles for that platform.
 */

html {
  background-color: <%= htmlWebpackPlugin.options.Styles.Colors.background %>;
}

body {
  overflow: hidden; /* Disables rubberband scrolling on MacOS. */
  margin: 0;
}

html, body, #root {
  height: 100%;
}
    </style>
  </head>
  <body>
    <div id="root"></div>
<!--
  We use this blanket polyfill that returns the minimum required script for
  the browser being used instead of hand selecting a set of polyfills which
  is errorprone and leads to us shipping code to browsers that we don’t have
  to ship.

  For more information see: https://polyfill.io/v2/docs/
-->
    <script src="https://cdn.polyfill.io/v2/polyfill.min.js"></script>
  </body>
</html>
